You are analyzing a sequence of frames from a LEGO assembly video to understand what action is being performed.

**SEQUENCE STRUCTURE:**
You will receive frames in this order:
1. PREVIOUS PLACEMENT FRAME: The last accepted placement state (hands clear, part placed)
2. ACTION FRAMES: ALL frames showing hands actively manipulating parts
3. CURRENT PLACEMENT FRAME: The current candidate placement state (hands clear, part placed)

**YOUR TASK:**

Analyze the action sequence to determine:
1. What action is being performed?
2. Is a new part being added, or is an existing part being adjusted?
3. What part appears to be involved (from action frames)?
4. What is the action narrative (describe what's happening)?

**ANALYSIS GUIDELINES:**

**Action Understanding:**
- Look at ALL action frames to understand the complete action
- If hands pick up a NEW part from outside the assembly → NEW placement
- If hands adjust/press an EXISTING part already on assembly → ADJUSTMENT (duplicate)
- If NO action frames between placements → likely DUPLICATE

**Part Identification (from action frames):**
- Describe what you see in the hands during action frames
- COLOR + TYPE + approximate SIZE (e.g., "dark grey brick, appears to be 2x2")
- Do NOT rely on final placement view - focus on what's in the hands during actions

**Note - Size Determination Using Placement Context:**
- Some LEGO pieces cannot be sized by counting visible studs:
  - **Smooth-top pieces (tiles)**: No visible studs on top
  - **Pieces with mismatched geometry**: Top stud count differs from bottom (e.g., brackets, inverted slopes)
- For these pieces, determine size using **PLACEMENT CONTEXT**:
  1. **Identify the surface underneath** - What brick/baseplate is it being placed on?
  2. **Measure relative coverage** - How much of the underlying surface does it cover?
  3. **Compare to adjacent pieces** - Use nearby parts as size reference
- If placement context is unclear or insufficient, return **"undetermined"** for the size rather than guessing

**Action Types:**
- "pickup_new_part" - Hands pick up a new part from outside assembly
- "place_new_part" - Hands place a new part onto assembly
- "adjust_existing" - Hands adjust/press/reposition existing part
- "no_action" - No meaningful action detected

**BOUNDING BOX (box_2d) - CRITICAL INSTRUCTIONS:**

**Step 1: Identify placement location from ACTION FRAMES**
- Look at the action frames and identify WHERE the hands are placing the new part
- Track the spatial location/position where the placement is happening
- Note visual landmarks around that location (adjacent parts, position on baseplate, etc.)

**Step 2: Compare PREVIOUS vs CURRENT frames**
- Look at the PREVIOUS PLACEMENT FRAME - memorize what parts are already there
- Look at the CURRENT PLACEMENT FRAME - identify what's NEW (not in previous frame)
- This helps when there are multiple similar parts visible

**Step 3: Draw bounding box on CURRENT PLACEMENT FRAME**
- Use the spatial location identified from action frames (Step 1)
- Draw the bounding box at that SAME location in the current placement frame
- The box should be around the part that's NEW (identified in Step 2)
- Format: [ymin, xmin, ymax, xmax] (values 0-1000, normalized coordinates)
- Draw a TIGHT box around the newly placed part only

**Example scenario:**
- If action frames show hands placing a red round brick on the RIGHT corner of the baseplate
- Then draw the bounding box around the red round brick that's on the RIGHT corner in the current frame
- NOT around other red bricks that may be elsewhere in the assembly

**If no new part or location unclear:**
- Set box_2d to null

**OUTPUT FORMAT:**

Return ONLY valid JSON:

```json
{
  "action_type": "place_new_part",
  "has_new_part": true,
  "part_from_actions": {
    "description": "green plate, appears to be 1x4",
    "color": "green",
    "type": "plate",
    "approximate_size": "1x4",
    "confidence": 0.85
  },
  "action_narrative": "Hands pick up a green plate from the side and attach it horizontally along the left edge of the assembly.",
  "confidence": 0.9,
  "reasoning": "Action frames show hands placing a green 1x4 plate at the LEFT edge of the baseplate. PREVIOUS frame had no green plate on the left edge. CURRENT frame shows a NEW green plate there. It is the leftmost piece in the assembly, to the left of the existing white brick. Normalized position: approximately xmin=650, xmax=750, ymin=280, ymax=320.",
  "box_2d": [280, 650, 320, 750]
}
```

**If NO new part (adjustment or duplicate):**

```json
{
  "action_type": "adjust_existing",
  "has_new_part": false,
  "part_from_actions": null,
  "action_narrative": "Hands press down on the existing dark grey brick to ensure it's seated properly.",
  "confidence": 1.0,
  "reasoning": "Action frames show hands pressing on a brick already present in the previous frame. No new part added.",
  "box_2d": null
}
```

**IMPORTANT:**
- Focus on understanding the ACTION from the sequence
- Describe what you see in the hands during action frames
- Distinguish between NEW part placement vs ADJUSTMENT of existing part
- **`reasoning` comes before `box_2d` — the coordinates must follow from the location you describe in reasoning, not the other way around**
- **When multiple similar parts exist: in the reasoning, identify which specific instance is new (e.g., "the rightmost one", "the one to the right of the last dark grey brick") before writing coordinates**
- This is Pass 2a - you're analyzing the action, not the final result
- Be conservative: if uncertain whether it's new or adjustment, describe what you see
- Do NOT include any text before or after the JSON
