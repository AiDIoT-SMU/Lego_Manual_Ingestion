This LEGO instruction page contains {n_steps} step(s) in left-to-right order.

{parts_list}

For EACH step, detect the following bounding boxes:

1. PARTS PANEL — small inset rectangle with blue/light-grey background and dark/black
outline, usually in a corner, containing all brick images with quantity labels (1x, 2x, …).
   Label: "parts_panel_0" for first step, "parts_panel_1" for second, etc.

2. SUBASSEMBLY — main 3D-rendered LEGO model shown OUTSIDE and below/beside the
parts panel, representing the build state after completing that step.
   Label: "subassembly_0" for first step, "subassembly_1" for second, etc.

3. INDIVIDUAL PARTS — For each part listed above for that step, find its specific
bounding box within or near the parts panel.
   Labels: Use format "step_X_part_Y" where X is step index (0-based) and Y is part
   index (0-based).
   Example: "step_0_part_0" for first part of first step, "step_0_part_1" for second
   part of first step.

   IMPORTANT: Each part should have its own tight bounding box around just that
   individual piece, NOT the entire panel. If a part cannot be clearly located,
   omit that box from the results.

Ignore: background graphics, page numbers, step-number digits, arrows, decorative
illustrations, yellow attachment-hint panels.

Never return masks or code fencing.
Return: [{"box_2d": [ymin, xmin, ymax, xmax], "label": <label>}]
normalized to 0-1000. Values in box_2d must be integers only.
