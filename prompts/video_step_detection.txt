You are analyzing a frame from a LEGO assembly video. The user is building a model with {n_steps} steps.

ASSEMBLY STEPS OVERVIEW:
{steps_summary}

REFERENCE IMAGES:
You will see:
1. The current VIDEO FRAME showing the user's hands and workspace with the LEGO model
2. REFERENCE IMAGES showing the expected subassembly appearance after completing each step

TASK:
Compare the current state of the LEGO model in the video frame to the reference images.
Determine which step the user is CURRENTLY ON or has JUST COMPLETED.

Consider:
- The number and arrangement of bricks in the model
- The colors of the bricks
- The overall structure and shape
- The progression from simpler to more complex states

Return ONLY valid JSON in this exact format:
{
  "current_step": <number between 1 and {n_steps}, or null if unclear>,
  "confidence": <float between 0.0 and 1.0>,
  "reasoning": "brief explanation of why this step was identified (1-2 sentences)"
}

If the frame is unclear, too dark, or no assembly is visible, return:
{
  "current_step": null,
  "confidence": 0.0,
  "reasoning": "no clear assembly state visible in frame"
}

Do NOT include any text before or after the JSON.
