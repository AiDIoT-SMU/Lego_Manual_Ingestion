You are generating clear, concise assembly instructions from video analysis data.

INPUT DATA:
Action type: {action_type}
Parts involved: {parts_list}
Spatial data: {spatial_json}

TASK:
Generate a single-sentence instruction that clearly describes this assembly action.

GUIDELINES:
1. Start with an imperative verb matching the action type:
   - PICK → "Pick up"
   - PLACE → "Place"
   - ATTACH → "Attach" or "Connect"
   - ADJUST → "Adjust" or "Reposition"
   - VERIFY → "Verify" or "Check"

2. Include part names exactly as provided in the parts list

3. Incorporate spatial details naturally when available:
   - Use location and position_detail from spatial data
   - Include orientation if specified
   - Reference relative positioning if provided

4. Be specific but concise (aim for 10-20 words)

5. Use proper LEGO terminology:
   - "studs" for connection points
   - "baseplate" for foundation pieces
   - "brick", "plate", "tile" for part types
   - Color + size + type pattern (e.g., "red 2x2 brick")

EXAMPLES:

Example 1 - PICK action:
Input: action_type="PICK", parts=["red round tile"], spatial=null
Output: "Pick up red round tile from workspace"

Example 2 - PLACE action with spatial detail:
Input: action_type="PLACE", parts=["red round tile", "grey baseplate"],
       spatial={location: "bottom left corner", position_detail: "2 studs from left edge and 3 studs from bottom edge"}
Output: "Place red round tile on grey baseplate at bottom left corner, 2 studs from left edge and 3 studs from bottom edge"

Example 3 - ATTACH action with orientation:
Input: action_type="ATTACH", parts=["white 1x2 brick", "grey baseplate"],
       spatial={location: "top row", position_detail: "3 studs from left edge", orientation: "horizontal"}
Output: "Attach white 1x2 brick horizontally in top row, 3 studs from left edge of grey baseplate"

Example 4 - PLACE action with relative positioning:
Input: action_type="PLACE", parts=["white 1x2 brick", "grey baseplate"],
       spatial={location: "top row", position_detail: "2 studs away", relative_to: "previous white brick"}
Output: "Place white 1x2 brick in same top row, 2 studs away from the previous white brick"

Example 5 - VERIFY action:
Input: action_type="VERIFY", parts=["assembly"], spatial=null
Output: "Verify that all bricks are securely attached"

Return ONLY valid JSON in this exact format:
{
  "description": "Place red round tile on grey baseplate at bottom left corner, 2 studs from left edge and 3 studs from bottom edge",
  "parts_mentioned": ["red round tile", "grey baseplate"],
  "spatial_terms_used": ["bottom left corner", "2 studs from left edge", "3 studs from bottom edge"]
}

Fields explanation:
- description: The complete instruction sentence
- parts_mentioned: Array of part names included in the description
- spatial_terms_used: Array of spatial terms/phrases used (empty array if no spatial detail)

Do NOT include any text before or after the JSON.
